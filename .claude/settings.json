{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=\"${CLAUDE_TOOL_INPUT_FILE_PATH:-}\"; case \"$f\" in content/zh/*|content/en/*|content/ja/*) echo \"→ wisdom-check  writing-check  accessibility-check\" ;; layouts/*|assets/css/*|tailwind*) echo \"→ visual-check\" ;; esac; exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=\"${CLAUDE_TOOL_INPUT_COMMAND:-}\"; case \"$cmd\" in *'git push'*' main'*|*'git push origin main'*) echo 'BLOCKED: push to main forbidden — use a feature branch' && exit 1 ;; *) exit 0 ;; esac"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(hugo*)",
      "Bash(npm run *)",
      "Bash(npm ci)",
      "Bash(npm install)",
      "Bash(go mod *)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git add*)",
      "Bash(git commit*)",
      "Bash(git checkout*)",
      "Bash(git fetch*)",
      "Bash(git pull*)",
      "Bash(git branch*)",
      "Bash(git push -u origin claude/*)",
      "Bash(git push -u origin feat/*)",
      "Bash(git push -u origin fix/*)"
    ],
    "deny": [
      "Bash(git push*main*)",
      "Bash(git push --force*)"
    ]
  }
}
